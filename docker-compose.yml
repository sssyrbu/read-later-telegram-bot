version: '3'
services:
  golang:
    build:
      context: .
      dockerfile: Dockerfile
    command: ./main
    depends_on:
      - redis

  redis:
    image: redis:latest
    ports:
      - "6379:6379"
